// Blog posts use plain rich text - StarterKit (in useCollaborativeEditor)
// already covers headings, lists, bold/italic, blockquotes, code blocks.
// This file exists so `formats/` has one entry per DocumentFormat and stays
// the single place to add blog-specific nodes later (e.g. pull-quotes,
// embeds) without touching the other formats.
export const blogExtensions = [];
